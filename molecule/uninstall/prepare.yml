---
- name: Prepare — clean environment
  hosts: all
  become: true
  tasks:
    - name: Remove pre-installed Go (GHA runner ships with Go)
      ansible.builtin.file:
        path: /usr/local/go
        state: absent
